{
  "name": "watch-video-skill",
  "version": "0.2.0",
  "private": true,
  "description": "Local helpers for the watch-video Codex skill.",
  "scripts": {
    "doctor": "python scripts/check_capabilities.py",
    "setup:node": "npm install && npx playwright install chromium",
    "watch:url": "node scripts/browser_watch_url.cjs"
  },
  "dependencies": {
    "playwright": "^1.56.0"
  }
}
